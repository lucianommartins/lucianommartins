name: Metrics

on:
  # Run this workflow automatically every 4 hours
  schedule:
    - cron: "0 */4 * * *"
  # Allow manual runs from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Required to push the generated SVG

    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          token: ${{ secrets.GITHUB_TOKEN }}
          
          # The filename for the output SVG
          filename: github-metrics.svg
          
          # Get metrics for your own user account
          user: lucianommartins
          
          # Use the classic template
          template: classic
          
          # CRITICAL FIX: Set the base to user-level sections to force a user-wide analysis.
          base: header, activity, community

          # === PLUGIN CONFIGURATION FOR A FULL USER OVERVIEW ===
          
          # Shows a 3D-style isometric calendar of your contributions
          plugin_isocalendar: yes
          
          # Shows lines of code added and removed across all your repos
          plugin_lines: yes
          
          # Shows your most used languages across all your repos
          plugin_languages: yes
          plugin_languages_limit: 8
          
          # Displays recent followers for community engagement
          plugin_people: yes
          plugin_people_types: followers
          
          # Configure timezone for accurate stats
          config_timezone: America/Sao_Paulo
